**【Title】** Evolving Cache Schedules for Fast Diffusion Policy Inference (arXiv:2607.20293, 22 Jul 2026)

- **一句话总结**：通过进化搜索全局优化扩散策略的缓存刷新调度，实现训练-free的显著推理加速，同时保持 visuomotor 性能。
- **核心贡献**（2-3点）：
  - 提出 EVO 框架，将调度建模为 block-timestep 格上的候选方案，使用进化搜索优化异构冗余（而非均匀分配）。
  - 训练-free，直接 plug-in 预训练扩散策略，无需重训。
  - 实验：在操纵基准上达 8.05x action-generation speedup，FLOPs 从 15.77G 降至 1.96G，接近全性能。
- **工程价值**：高度相关推理优化（类似 KV cache 管理），适用于实时机器人/扩散-based 部署；进化搜索成本可离线 amortize，易集成到 serving pipeline。
- **是否值得读**：**强烈推荐**（系统优化 + 实测 speedup + 可复现线索）。
- **Relevance Score**：5/5
- **Link**：https://arxiv.org/abs/2607.20293

**【Title】** How Fast Can Reward Models Score? A Systems Study of C++ and PyTorch Inference Runtimes for RLHF (arXiv:2607.19712, 22 Jul 2026)

- **一句话总结**：系统对比 C++/ONNX Runtime vs PyTorch 在 RLHF reward scoring 上的推理性能，揭示 batching 和 runtime 选择对 throughput 的关键影响。
- **核心贡献**：
  - 构建原生 C++ 引擎（ONNX），CPU/GPU 基准测试，确认正确性并量化 speedup。
  - 发现 batching 策略比语言/runtime 本身更重要；ONNX 在 CPU 上显著胜出。
  - 针对 RLHF 瓶颈（scoring 阻塞 policy update）的实用洞见。
- **工程价值**：直接服务 RLHF/SFT 流水线优化，降低 reward model 延迟，提升整体训练 throughput；生产 serving 相关。
- **是否值得读**：**强烈推荐**（工程实证 + 系统对比）。
- **Relevance Score**：5/5
- **Link**：https://arxiv.org/abs/2607.19712

**【Title】** SLPO: Scaling Latent Reasoning via a Surrogate Policy (arXiv:2607.19691, 22 Jul 2026)

- **一句话总结**：针对 latent reasoning（连续向量中间计算）引入 surrogate policy optimization，实现 outcome-reward RL 下的 test-time scaling 和 adaptive stopping。
- **核心贡献**：
  - 提出 surrogate policy density 用于 trajectory-level credit assignment + correctness-supervised stopping head。
  - 在 continuous/soft thinking 设置下提升 Pass@k，并为难例分配更多计算。
  - 填补 latent reasoner 在 RL 上的空白（此前多限于 imitation）。
- **工程价值**：LLM Agents / RL 方向；潜在降低显式 CoT 开销，支持高效 agent 推理；有 benchmark 支撑。
- **是否值得读**：**强烈推荐**（方法创新 + RL 落地）。
- **Relevance Score**：4/5
- **Link**：https://arxiv.org/abs/2607.19691

**【Title】** ISO: An RLVR-Native Optimization Stack (arXiv:2607.19331, ~22 Jul 2026)

- **一句话总结**：分析 RLVR 中 singular value spectra 与 frames 的作用，提出 ISO-Merger（offline expert 合并）和 ISO-Optimizer（online 针对 frames），加速 RLVR 收敛。
- **核心贡献**：
  - 发现 RL 能力主要在 frames（U,V），spectrum 基本不变；提出相应 merge/optimize 技术。
  - ISO-Optimizer 可 2.7x 减少步骤匹配 RLVR 精度。
  - 无需额外 data/gradients 的 offline 合并胜过多种 baselines。
- **工程价值**：SFT/Alignment/RLHF 强相关；优化 RLVR 训练效率，易集成[118;1:3u到 post-training 栈。
- **是否值得读**：**强烈推荐**。
- **Relevance Score**：5/5
- **Link**：https://arxiv.org/abs/2607.19331 (或 HF 页面)

**【Title】** Distilled Reinforcement Learning for LLM Post-training (arXiv:2607.17247, ~21-22 Jul)

- **一句话总结**：Distilled RL 将 teacher 用于 token-level 选择性 policy-gradient 重分布，结合 outcome reward，提供细粒度指导，提升数学推理。
- **核心贡献**：
  - 结合 reverse importance sampling 等组件，优于纯 OPD 或 RL。
  - 在小模型（1.5B-4B）上显著提升 Pass@1（e.g., Qwen3-4B 达 58.96%）。
- **工程价值**：SFT/Alignment/RLHF；改善 RL 信号稀疏问题，适用于 post-training 流水线。
- **是否值得读**：**强烈推荐**（实验支撑 + 实用）。
- **Relevance Score**：4/5
- **Link**：https://arxiv.org/abs/2607.17247

其他潜在候选（如 UltraQuant KV caching for agents、RoPE-aware KV quant）多为稍早日期或非过去48h 严格新发，已过滤。纯理论/无强工程 benchmark 的跳过。

### 1. 今日最值得读 Top 3（理由）
1. **ISO (RLVR Optimization)**：直接针对 RLVR（当前热门）优化栈，spectra/frames 洞见 + 实测加速，工程落地潜力最大。
2. **Evolving Cache Schedules (EVO)**：推理优化典范，8x+ speedup + training-free，立即可用于 diffusion/类似 cache-heavy 部署。
3. **Reward Models Scoring Systems Study**：RLHF 核心瓶颈的系统实证，batching/runtime 洞见对 infra 工程师极具价值。

### 2. 今日趋势观察
Inference 优化（尤其是 KV/cache 调度、quantization 在 agents/reasoning 中的应用）仍强势，但过去48h 内 RL/RLHF 相关（latent scaling、distilled RL、RLVR-native stack）明显增加，反映 post-training 与 reasoning scaling 的工程关注度提升。Agent 方向有 latent/privileged info 进展，但数量有限。

### 3. 是否出现“值得跟进的潜在方向”
是的：**RLVR-native optimization + distillation hybrids**（结合 spectra insight、token-level guidance 与 latent reasoning）。这可能成为高效 alignment/agents scaling 的新范式，值得跟踪 spectra-aware merging 在分布式训练/ serving 中的扩展，以及与 speculative/KV 优化的交叉。宁缺毋滥，未达门槛的其他方向暂不列。
